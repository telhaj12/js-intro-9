const numbers = [ 100, 50, 25,500];
// first element is always index of zero
console.log(numbers[0]);

// last element is always at the index of array.length-1
console.log(numbers[numbers.length - 1]);
console.log(numbers.at(-1));

