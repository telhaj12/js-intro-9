//CONCATENATION


let fname = 'John';

// length property - returns how many characters you have in the string 
 /*let sizeFname = fname.length*/

 console.log(sizeName); // 4 
 
 or

 console.log(fname.length); // 4
 // using only fname.length without storing in a variable is best if you're only using it once

 // concat() methods

 let lname = 'Wick';

 let fullname = fname + lname;
 console.log(fullname);

 // let fullname = fnmae + ' ' + lname; 
 // let fullname = `${fname} ${lname}`;
 // let fullname = fname.concat(' ').concat(lname); // 'John Wick'


  // This is an interview question, how many diffferent ways to concatenate - 4


  console.log(fname, lname);
  /*
  CONCAT METHOD
  TASK: it combines strings
  RETURN: it returns a new string
  ARGUMENTS: it takes a string as an argument 
  CHANGE ORIGINAL VALUE: it does not change the original string values
  STATIC or NON STATIC: non-static
  
  
  STATIC V NON STATIC decision for properties and methods
  Did you call method or property with variable?
  YES -> NON-STATIC
  NO -> STATIC
*/


Math.max()
Math.min()
Math.random()
// These are all static




